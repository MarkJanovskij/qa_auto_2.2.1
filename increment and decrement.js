

function incrementAndDecrement(x, y) {
    x++;
    y--;
    console.log("x after increment: " + x); // Output: x after increment: 
    console.log("y after decrement: " + y); // Output: y after decrement: 
  }

  incrementAndDecrement(5, 5); // Output: x after increment: 6, y after decrement: 

  function increment(x) {
    x ++;
    return x;
    }
    
    console.log(increment(5))

    function decrement(x) {
        x --;
        return x;
        }
        
        console.log(decrement(10))
    // update add increment and decrement dunction